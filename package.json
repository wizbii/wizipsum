{
  "private": true,
  "scripts": {
    "fetch": "node fetch.js",
    "zip": "bestzip extension.zip icon.png popup.html manifest.json public/",
    "styles": "rm public/css/* && node-sass src/scss/styles.scss --output-style compressed | postcss --use autoprefixer --output public/css/styles.css",
    "prebuild": "npm run styles",
    "build": "rm public/scripts/* && webpack -p",
    "postbuild": "npm run zip"
  },
  "devDependencies": {
    "autoprefixer": "*",
    "bestzip": "*",
    "json-loader": "*",
    "node-sass": "*",
    "postcss": "*",
    "postcss-cli": "*",
    "request": "*",
    "webpack": "*",
    "wizipsum": "*"
  }
}
