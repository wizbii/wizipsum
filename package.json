{
  "private": true,
  "scripts": {
    "zip": "bestzip extension.zip icon.png popup.html manifest.json css/ scripts/",
    "styles": "rm css/* && node-sass scss/styles.scss --output-style compressed | postcss --use autoprefixer --output css/styles.css",
    "prebuild": "npm run styles",
    "build": "rm scripts/* && webpack -p",
    "postbuild": "npm run zip"
  },
  "devDependencies": {
    "autoprefixer": "*",
    "bestzip": "*",
    "node-sass": "*",
    "postcss": "*",
    "postcss-cli": "*",
    "webpack": "*",
    "wizipsum": "*"
  }
}
